def weekday_name(day_of_week):
    days = ['Sunday','Monday','Tuesday','Wendnesday','Thursday','Friday','Saturday']
    if day_of_week >= 1 and day_of_week < len.(days)
            return days[day_of_week]
        else: return 'none'
            